// DOCE.js - Display Object Circuit Element
DOCE.X = 0;

function DOCE(style){
	var self = this;
	Code.extendClass(this,DOContainer,arguments);
	/*self.element = new DO(style.element);
	self.display.addChild(element);
	self.pins = new Array();
	var pinList = style.pins;
	var i, img;
	for(i=0;i<pinList.length;++i){
		img = new DOImage(pinList[i]);
		self.pins.push(img);
		self.display.addChild(img);
	}
	*/
	//
	// YAY
}

